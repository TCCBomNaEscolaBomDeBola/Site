@model IEnumerable<Araretama.BomNaEscolaBomDeBola.Domain.Turma>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}

<p>
    <h2>Turmas</h2>
</p>
@if (Model != null)
{
    <table id="datatable-responsive" class="table table-striped table-bordered dt-responsive nowrap jambo_table" cellspacing="0" width="100%">
        <thead>
            <tr class="headings">
                <th>
                    @Html.DisplayNameFor(model => model.Nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.IdadeMinima)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.IdadeMaxima)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.QuantidadeDeAlunos)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.HorarioInicial)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.HorarioFinal)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DiaSemana)
                </th>
                <th></th>
            </tr>
        </thead>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdadeMinima)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdadeMaxima)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.QuantidadeDeAlunos)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HorarioInicial)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HorarioFinal)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DiaSemana)
                </td>
                <td>


                    <a class="btn btn-primary" href="@Url.Action("Details", "Turma", new { id = item.Id })"><i class="fa fa-plus"></i></a>
                    <a class="btn btn-warning" href="@Url.Action("Edit", "Turma", new { id = item.Id })"><i class="fa fa-pencil"></i></a>
                    <a class="btn btn-danger" href="@Url.Action("Delete", "Turma", new { id = item.Id })"><i class="fa fa-trash"></i></a>

                </td>
            </tr>
        }

    </table>
}
